import type { Metadata } from "next";

export const metadata: Metadata = { title: "Tài khoản của tôi" };

export default function ProfilePage() {
  return (
    <div className="container py-8">
      {/* ProfileTabs (Info / Addresses / Security) — Phase 2 */}
    </div>
  );
}
