import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Giỏ hàng",
};

export default function CartPage() {
  return (
    <div className="container py-8">
      {/* CartItemList — Phase 2 */}
      {/* CartSummary — Phase 2 */}
    </div>
  );
}
