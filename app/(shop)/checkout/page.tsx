import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Thanh toán",
};

export default function CheckoutPage() {
  return (
    <div className="container py-8">
      {/* AddressSelector — Phase 2 */}
      {/* PaymentMethodSelector — Phase 2 */}
      {/* CouponInput — Phase 2 */}
      {/* OrderSummary — Phase 2 */}
      {/* PlaceOrderButton — Phase 2 */}
    </div>
  );
}
