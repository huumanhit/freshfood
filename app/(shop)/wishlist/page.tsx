import type { Metadata } from "next";

export const metadata: Metadata = { title: "Danh sách yêu thích" };

export default function WishlistPage() {
  return (
    <div className="container py-8">
      {/* WishlistGrid — Phase 2 */}
    </div>
  );
}
